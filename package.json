{
  "name": "blog.application",
  "version": "1.0.0",
  "description": "Your project description",
  "scripts": {
    "start": "concurrently \"npm run frontend\" \"npm run backend\"",
    "frontend": "cd frontend/my-app && npm run dev",
    "backend": "cd backend && nodemon server.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "nodemon": "^3.0.1"
  },
  "dependencies": {
    "bcrypt": "^5.1.0",
    "dotenv": "^16.3.1",
    "jsonwebtoken": "^9.0.1",
    "mongoose": "^7.4.2"
  }
}
